import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcuscastelo.macroflows',
  appName: 'Macroflows',
  webDir: '.output/public'
};

export default config;
